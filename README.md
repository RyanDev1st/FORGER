configure PLAYWRIGHT_MCP_EXECUTABLE_PATH with cloakbrowser executable path, for example:

                                                                                                                                                                           
Do not touch any other files, work with /.results folder only.  
```bash
export PLAYWRIGHT_MCP_EXECUTABLE_PATH=/path/to/cloakbrowser
```

Then you can use `playwright-cli` to control the browser:

```bash